# `cm add-user`

Legacy shorthand for saving a project-local user preference.

```bash
cm add-user "Use compact progress updates in this repository."
```

Use `cm save --global --kind preference --layer user` for a general preference
that should be visible in every project.
