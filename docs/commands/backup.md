# `cm backup`

Write a backup of the current store.

```bash
cm backup
cm backup --global
```

Global backups are JSON files written in the current directory. Project backups
are written under the project backup path.
