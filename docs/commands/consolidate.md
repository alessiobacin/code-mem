# `cm consolidate`

Promote working/episodic intake, normalize summaries, and ensure vectors.

```bash
cm consolidate
cm consolidate --accept-candidates
cm consolidate --prune
```

Run after a meaningful implementation or debugging session.
