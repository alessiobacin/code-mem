# `cm graph`

Graph command namespace alias.

```bash
cm graph serve
```

Use `cm serve` for the normal graph URL. Other graph operations use their
short commands (`cm ga`, `cm ge`, `cm gn`, `cm gp`, `cm gc`, `cm gx`, `cm gs`,
and `cm gi`).
