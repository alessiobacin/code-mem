# `cm help`

Print the concise or complete command reference.

```bash
cm help
cm help --full
cm --full
```

The lean form keeps the common memory workflow visible; `--full` exposes
diagnostic graph, scan, import, and semantic commands.
