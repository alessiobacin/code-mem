# `cm link`

Create a typed relationship between two memory rows.

```bash
cm link <source-id> <target-id> supports 1
```

Links expand recall context and are stored in the project or global database
where the command runs.
