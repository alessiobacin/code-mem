# `cm project`

Regenerate `memory/MEMORY.md` and `memory/USER.md` from SQLite.

```bash
cm project
cm project --compact
```

The Markdown files are projections; edit the database through `cm` commands.
