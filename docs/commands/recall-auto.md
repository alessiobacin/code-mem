# `cm recall-auto`

Build and print session-start context from the current repository.

```bash
cm recall-auto
```

Harness hooks call this path automatically. It also records the current context
in the capture layer.
