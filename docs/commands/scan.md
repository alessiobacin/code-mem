# `cm scan`

Run lower-level diagnostic scans. The deep unified workflow includes these
steps automatically.

```bash
cm scan --deep [--no-ast]
cm scan --relations
cm scan --relations --apply
```

Use `cm init --deep` or `cm update --memory --deep` for the complete path.
