# `cm serve`

Open the current project's 3D graph through the global local service.

```bash
cm serve
cm serve --foreground --port 4805
```

The foreground form is a diagnostic project-local server. The normal form uses
the project id and capability token in the loopback URL.
