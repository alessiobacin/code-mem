# `cm setup`

Install the Code-Mem skill and supported user-level harness integration.

```bash
cm setup
```

Run this outside a repository for global skill setup. Project hooks are
installed by `cm init --deep` or `cm update --memory` inside a project.
