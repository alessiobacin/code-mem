{
  "name": "code-mem",
  "version": "0.8.1",
  "private": true,
  "type": "module",
  "description": "Local-first persistent project memory for coding agents.",
  "scripts": {
    "build": "node build/bundle.mjs",
    "lint": "for f in src/*.js; do node --check \"$f\" || exit 1; done; echo LINT-OK",
    "test": "node --test tests/non-regression/cm-cli.test.mjs && bash tests/test-e2e.sh && bash tests/tddb/run-all.sh"
  }
}
